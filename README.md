# schemashift
A Python tool that watches a directory of JSON/CSV data files, detects schema changes over time, and renders an interactive terminal diff showing what columns/fields were added, removed, renamed, or type-changed between versions.
